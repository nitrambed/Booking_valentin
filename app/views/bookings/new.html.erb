<h1>Your are booking <%= @valentin.name %></h1>
<%= simple_form_for @booking do |f| %>
  <%= f.hidden_field :valentin_id, value: @valentin.id %>
  <%= f.input :reservation_at, as: :date, html5: true %>
  <%= f.submit "Submit", class: "btn btn-primary" %>
<% end %>
